package core.ui.layout;

import core.ui.Container;
import core.ui.IComponent;

/**
 * Alignments represent an alignment of an {@link IComponent} inside it's {@link Container}.
 * 
 * @see AbstractLayout
 */
public enum Alignment {

    LEFT, RIGHT,
    TOP, BOTTOM,
    CENTER
    
}